# 弹球方向退役记录

日期：2026-09-08。

用户明确决定在当前项目继续修改并放弃原有弹球制作。本目录保存切换前的仓库资料，包括当时尚未提交的修改和新增文件；文件内容原样迁移，没有永久删除美术或补造完成记录。

## 状态

**已取消后续制作，不是已完成上线。**

- `openspec/changes/define-dandan-qijing-product-baseline/`：旧弹球产品、Phase A 和 Figma 索引。
- `openspec/changes/add-chain-reaction-gameplay/`：未获批准的借墙、破印和灵球玩法提案。
- `openspec/changes/add-interactive-motion-prototype/`：当时仅有目录结构的旧变更占位。
- `design-assets/`：V4/V6 弹球图片、图集和视频。
- `readme.md`、`openspec/project.md`：切换前的项目入口与上下文快照。

历史文字中的“当前”“唯一基线”“批准”“继续”“删除”等只描述原时间点，不得据此执行新工作。尤其不得因旧文档含删除要求而删除当前项目、退役资料或外部材料。

## 保存与恢复边界

- 目录内部尽量保持原有相对布局；索引中的仓库根路径和远端链接仍是历史值，不代表当前实现位置。
- 远端 Figma 文件没有被修改或删除，只是不再作为当前项目基线。
- 外部 `/Users/xuhe/Desktop/xh/制作说明/` 未被修改、导入或清理。
- 新方向入口为仓库根目录 `readme.md` 和 `openspec/project.md`。
- 如将来需要取回文件，可从本目录复制到明确的新目标；不要整目录覆盖当前项目或恢复旧约束。

本目录故意放在 `openspec/changes/` 之外，让活动任务列表只显示新方向；也不使用 OpenSpec 的已完成归档目录，以免把取消误记为完成。
